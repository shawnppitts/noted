<div class="wrapper_with_padding">
  <div class="clearfix" id="notes">
    <% unless @notes.blank? %>
      <% @notes.each do |note| %>
        <a href="<%= (url_for [note]) %>">
          <div class="note">
            <p class="title">
              <%= note.title %>
            </p>
            <p class="date">
              <%= time_ago_in_words(note.created_at) %>
            </p>
          </div>
        </a>
      <% end %>
    <% else %>
      <h2>Add Note</h2>
      <p>You have no current notes.</p>
      <button>
        <%= link_to "New Note", new_note_path %>
      </button>
    <% end %>
  </div>
</div>
